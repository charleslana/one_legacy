import 'package:flutter/material.dart';

class HomeTeamPage extends StatelessWidget {
  const HomeTeamPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Team'),
    );
  }
}
