import 'package:flutter/material.dart';

class HomeUnitsPage extends StatelessWidget {
  const HomeUnitsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Units'),
    );
  }
}
